# 🚀 Quick Start - WebSocket Version Installation

## ⚡ Быстрая установка за 3 шага

### **Шаг 1: Замените файлы**

В вашем проекте `crypto-screener-bybit`, замените следующие файлы:

```bash
# 1. Скопируйте новый файл
cp websocket-version/backend/screener/websocket_manager.py \
   I:/crypto-screener-bybit/backend/screener/

# 2. Замените engine.py
cp websocket-version/backend/screener/engine.py \
   I:/crypto-screener-bybit/backend/screener/

# 3. Замените filters.py  
cp websocket-version/backend/screener/filters.py \
   I:/crypto-screener-bybit/backend/screener/
```

### **Шаг 2: Добавьте настройку в .env**

Откройте `.env` и добавьте (если нет):

```bash
CHECK_DELAY_SECONDS=10
```

### **Шаг 3: Перезапустите**

```bash
# Остановить текущую версию
docker-compose down

# Пересобрать и запустить
docker-compose up -d --build

# Проверить логи
docker-compose logs -f backend
```

---

## ✅ Проверка работы

Вы должны увидеть в логах:

```
🚀 STARTING CRYPTO SCREENER (WEBSOCKET MODE)
📦 Initializing database...
🔌 Initializing exchange...
📱 Initializing Telegram notifier...
🔍 Loading active filters...
📡 Starting WebSocket manager for X symbols...
📡 Starting WebSocket watch for BTC/USDT
🔔 Candle closed: 11:32:00. Queuing symbols for filter checks...
```

---

## 🎯 Что изменилось?

### **БЫЛО (REST):**

```
11:30:00 - Начало цикла
11:30:05 - Загрузка тикеров (10 сек)
11:34:30 - Загрузка свечей (4.5 минуты)
11:34:35 - Проверка фильтров (1 сек)
11:35:00 - Сон до следующего цикла (5 минут)
```

**Задержка: 0-5 минут**

### **СТАЛО (WebSocket):**

```
11:32:00 - Свеча закрылась
11:32:10 - ✅ Проверка фильтров (сразу!)
11:33:00 - Следующая свеча закрылась
11:33:10 - ✅ Проверка фильтров
```

**Задержка: 10 секунд!** 🚀

---

## 📊 Быстрая диагностика

### **Проблема: WebSocket не подключается**

```bash
# Проверить логи
docker-compose logs backend | grep "WebSocket error"

# Решение:
# 1. Проверить VPN
# 2. Проверить интернет
# 3. Подождать 1-2 минуты (retry автоматический)
```

### **Проблема: Фильтры не срабатывают**

```bash
# Проверить что фильтры активны
docker-compose logs backend | grep "active filter"

# Проверить что свечи закрываются
docker-compose logs backend | grep "Candle closed"

# Проверить проверки фильтров
docker-compose logs backend | grep "Checking.*filter"
```

### **Проблема: Пропуски в данных (gaps)**

```bash
# Gap recovery работает автоматически
docker-compose logs backend | grep -i "gap"

# Должны быть:
# ⚠️ Gap detected: X minutes missing
# ✅ Gap filled: restored X candles
```

---

## 🔥 Важные отличия от REST версии

| Аспект | REST (старое) | WebSocket (новое) |
|--------|---------------|-------------------|
| **Обновление данных** | Каждые 5 минут | Каждую секунду |
| **Проверка фильтров** | Раз в 5 минут | Каждую минуту |
| **Задержка** | 0-5 минут | < 10 секунд |
| **Rate limits** | ~600 req/цикл | 0 (WebSocket) |
| **Пропуски данных** | Возможны | Auto-recovery |
| **Нагрузка на БД** | Средняя | Средняя (батчинг) |

---

## 📈 Следующие шаги

1. **Дать поработать 1-2 часа** - понаблюдать за логами
2. **Создать фильтры** - через веб-интерфейс
3. **Проверить уведомления** - в Telegram
4. **Мониторить производительность** - через логи

---

## 🆘 Если что-то не работает

### **1. Проверить логи**

```bash
# Общие логи
docker-compose logs -f backend

# Только ошибки
docker-compose logs backend | grep -i error

# Только WebSocket
docker-compose logs backend | grep -i websocket
```

### **2. Перезапустить**

```bash
# Полный рестарт
docker-compose down
docker-compose up -d --build
```

### **3. Проверить настройки**

```bash
# .env должен содержать:
cat .env | grep TELEGRAM_BOT_TOKEN  # Заполнено?
cat .env | grep TELEGRAM_CHAT_ID    # Заполнено?
cat .env | grep CHECK_DELAY_SECONDS # Есть?
```

---

## 💡 Полезные команды

```bash
# Статус контейнеров
docker-compose ps

# Логи последних 100 строк
docker-compose logs --tail=100 backend

# Логи за последний час
docker-compose logs --since 1h backend

# Количество WebSocket подключений
docker-compose logs backend | grep "WebSocket watch" | wc -l

# Количество проверок фильтров за час
docker-compose logs backend --since 1h | grep "Checking.*filter" | wc -l

# Срабатывания за сегодня
docker-compose logs backend --since today | grep "TRIGGERED" | wc -l
```

---

## ✅ Контрольный список

- [ ] Файлы скопированы
- [ ] .env настроен (CHECK_DELAY_SECONDS=10)
- [ ] Docker перезапущен
- [ ] В логах видно "WEBSOCKET MODE"
- [ ] WebSocket подключения установлены
- [ ] Свечи закрываются каждую минуту
- [ ] Фильтры проверяются
- [ ] Telegram работает

---

**Готово! Ваш скринер теперь работает в real-time! 🚀**
