# Contributing to Crypto Screener

Спасибо за интерес к улучшению проекта! 🎉

## 📋 Перед началом

Прочитайте документацию:
- [README.md](README.md) - общая информация
- [TECHNICAL_SPECIFICATION_V2.md](TECHNICAL_SPECIFICATION_V2.md) - техническая спецификация
- [CODE_REQUIREMENTS.md](CODE_REQUIREMENTS.md) - стандарты кодирования

## 🔧 Настройка окружения разработки

### 1. Fork и Clone

```bash
# Fork репозиторий на GitHub

# Clone
git clone https://github.com/your-username/crypto-screener.git
cd crypto-screener

# Добавить upstream
git remote add upstream https://github.com/original-repo/crypto-screener.git
```

### 2. Создать .env

```bash
cp .env.example .env
# Заполнить TELEGRAM_BOT_TOKEN и TELEGRAM_CHAT_ID
```

### 3. Запустить в dev режиме

```bash
docker-compose up --build
```

## 📝 Стандарты кодирования

### Python

- **PEP8** соблюдение обязательно
- **Type hints** для всех функций
- **Docstrings** в Google style
- **Логирование** на всех уровнях (DEBUG, INFO, WARNING, ERROR)
- **Error handling** - всегда try/except с логированием

### Пример:

```python
async def fetch_candles(
    symbol: str,
    market: str,
    timeframe: str = '1m'
) -> list[dict]:
    """
    Fetch candles from exchange.
    
    Args:
        symbol: Trading pair (e.g. 'BTC/USDT')
        market: 'spot' or 'futures'
        timeframe: Timeframe string (default: '1m')
    
    Returns:
        List of candle dicts
    
    Raises:
        NetworkError: If exchange unreachable
    """
    try:
        logger.debug(f"Fetching {timeframe} candles for {symbol} ({market})")
        # ... implementation
    except Exception as e:
        logger.error(f"Error fetching candles: {e}", exc_info=True)
        raise
```

### JavaScript

- **ES6+** синтаксис
- **Async/await** для асинхронных операций
- **Комментарии** для сложной логики

## 🌿 Workflow

### 1. Создать ветку

```bash
git checkout -b feature/your-feature-name
# или
git checkout -b fix/bug-description
```

### 2. Внести изменения

- Следовать стандартам кодирования
- Добавить комментарии где необходимо
- Обновить документацию если нужно

### 3. Тестирование

```bash
# Запустить систему
docker-compose up --build

# Проверить логи
docker-compose logs -f backend

# Проверить функциональность
curl http://localhost:8000/health
```

### 4. Commit

```bash
git add .
git commit -m "feat: Add new filter type"
```

**Формат commit messages:**
- `feat:` - новая функциональность
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `refactor:` - рефакторинг кода
- `test:` - добавление тестов
- `chore:` - изменения в конфигурации

### 5. Push и Pull Request

```bash
git push origin feature/your-feature-name
```

Создать Pull Request на GitHub с описанием:
- Что изменено
- Зачем
- Как тестировать

## 🧪 Тестирование

### Обязательно проверить:

1. **Основной функционал:**
   - [ ] Парсинг работает
   - [ ] Фильтры проверяются
   - [ ] Telegram уведомления приходят
   - [ ] WebSocket работает

2. **Критические требования:**
   - [ ] Timestamps в секундах
   - [ ] Спот/Фьючерсы раздельно
   - [ ] quoteVolume используется
   - [ ] Cooldown работает

3. **UI/UX:**
   - [ ] Страницы загружаются
   - [ ] Формы работают
   - [ ] Real-time обновления работают

### Используй чек-лист из:

[CRITICAL_CHECKS.md](CRITICAL_CHECKS.md)

## 📖 Что можно улучшить

### Высокий приоритет:

- [ ] Unit тесты (pytest)
- [ ] E2E тесты
- [ ] CI/CD pipeline (GitHub Actions)
- [ ] Более детальная документация API

### Средний приоритет:

- [ ] Поддержка других бирж (Binance, OKX)
- [ ] Множественные Telegram чаты
- [ ] Dashboard с графиками
- [ ] Экспорт данных в CSV

### Низкий приоритет:

- [ ] Mobile app
- [ ] Multi-user support
- [ ] Backtesting фильтров
- [ ] AI рекомендации

## 🐛 Сообщение о багах

### Создать Issue с:

1. **Описание проблемы**
2. **Шаги воспроизведения**
3. **Ожидаемое поведение**
4. **Фактическое поведение**
5. **Логи:**
   ```bash
   docker-compose logs backend --tail=50
   ```
6. **Окружение:**
   - OS
   - Docker version
   - Browser (для UI багов)

## 💡 Предложения функций

### Создать Issue с:

1. **Описание функции**
2. **Use case** - зачем нужна
3. **Предложенная реализация** (опционально)
4. **Альтернативы** (опционально)

## ❓ Вопросы

Есть вопросы? Создайте Issue с тегом `question`

## 📜 Code of Conduct

- Уважайте других участников
- Конструктивная критика
- Помогайте новичкам
- Никакого спама

## ✅ Checklist перед Pull Request

- [ ] Код следует стандартам ([CODE_REQUIREMENTS.md](CODE_REQUIREMENTS.md))
- [ ] Добавлены type hints
- [ ] Добавлены docstrings
- [ ] Добавлено логирование
- [ ] Обработка ошибок (try/except)
- [ ] Протестировано локально
- [ ] Документация обновлена (если нужно)
- [ ] Commit messages в правильном формате
- [ ] Нет конфликтов с main

## 📞 Контакты

- GitHub Issues - основной канал
- Email - для приватных вопросов

---

Спасибо за вклад в проект! 🚀
